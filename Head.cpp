#include <bits/stdc++.h>
#pragma GCC optimize("Ofast")
#pragma GCC optimize("unroll-loops")
using namespace std;
struct __INIT{__INIT(){cin.tie(0);ios::sync_with_stdio(false);cout<<fixed<<(15);}};
typedef long long lint;
typedef __int128_t llint;
#define INF (1 << 30)
#define LINF (lint)(1LL << 56)
#define MINF (lint)(2e18)
#define endl "\n"
#define rep(i, n) for (lint(i) = 0; (i) < (n); (i)++)
#define rep_1(i, n) for (lint(i) = 1; (i) <= (n); (i)++)
#define yesret() cout<<"Yes"<< endl; return 0;
#define noret() cout<<"No"<< endl; return 0;
#define all(x) x.begin(), x.end()
#define vec vector<lint>
const int MAX_N = 3e5 + 5;
// constexpr int MOD=1000000007;
// constexpr int MOD=998244353;

int main() {

    return 0;
}
